#include <iostream>

int main(){

    /* type conversion
        implicit = automatic (no need of preceding values)
        explicit = precede value with new data type
    */

   double x = 3.14;

   // making it into a new data type
   int z = (int)x;

   std::cout << x;  

}